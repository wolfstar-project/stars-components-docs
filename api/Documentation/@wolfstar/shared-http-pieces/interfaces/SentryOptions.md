[@wolfstar/website](../../../../index.md) / [Documentation](../../../index.md) / [@wolfstar/shared-http-pieces](../index.md) / SentryOptions

# Interface: SentryOptions

Defined in: [packages/shared-http-pieces/src/lib/sentry.ts:51](https://github.com/wolfstar-project/stars-components/blob/f7b31cc0eb5f9a284e64590e28eadbf470e88f64/packages/shared-http-pieces/src/lib/sentry.ts#L51)

## theme_extends

- `Omit`\<[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md), `"dsn"`\>

## Properties

### \_experiments?

> `optional` **\_experiments?**: `object`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:390

Options which are in beta, or otherwise not guaranteed to be stable.

#### Index Signature

\[`key`: `string`\]: `any`

#### ~~beforeSendMetric?~~

> `optional` **beforeSendMetric?**: (`metric`) => [`Metric`](../namespaces/Sentry/interfaces/Metric.md) \| `null`

**`Experimental`**

An event-processing callback for metrics, guaranteed to be invoked after all other metric
processors. This allows a metric to be modified or dropped before it's sent.

Note that you must return a valid metric from this callback. If you do not wish to modify the metric, simply return
it at the end. Returning `null` will cause the metric to be dropped.

##### Parameters

###### metric

[`Metric`](../namespaces/Sentry/interfaces/Metric.md)

The metric generated by the SDK.

##### Returns

[`Metric`](../namespaces/Sentry/interfaces/Metric.md) \| `null`

A new metric that will be sent | null.

##### Default

```ts
undefined
@experimental
```

##### Deprecated

Use the top level`beforeSendMetric` option instead.

#### ~~enableLogs?~~

> `optional` **enableLogs?**: `boolean`

Determines if logs support should be enabled.

##### Default

```ts
false;
```

##### Deprecated

Use the top level `enableLogs` option instead.

#### ~~enableMetrics?~~

> `optional` **enableMetrics?**: `boolean`

**`Experimental`**

If metrics support should be enabled.

##### Default

```ts
false
@experimental
```

##### Deprecated

Use the top level`enableMetrics` option instead.

#### Inherited from

`Omit._experiments`

---

### allowUrls?

> `optional` **allowUrls?**: (`string` \| `RegExp`)[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:433

A pattern for error URLs which should exclusively be sent to Sentry.
This is the opposite of [CoreOptions.denyUrls](#denyurls).
By default, all errors will be sent.

Behavior of the `allowUrls` option is controlled by the `Sentry.eventFiltersIntegration` integration.
If the event filters integration is not installed, the `allowUrls` option will not have any effect.

#### Default

```ts
[];
```

#### Inherited from

`Omit.allowUrls`

---

### attachStacktrace?

> `optional` **attachStacktrace?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:175

When enabled, stack traces are automatically attached to all events captured with `Sentry.captureMessage`.

Grouping in Sentry is different for events with stack traces and without. As a result, you will get
new groups as you enable or disable this flag for certain events.

#### Default

```ts
false;
```

#### Inherited from

`Omit.attachStacktrace`

---

### beforeBreadcrumb?

> `optional` **beforeBreadcrumb?**: (`breadcrumb`, `hint?`) => [`Breadcrumb`](../namespaces/Sentry/interfaces/Breadcrumb.md) \| `null`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:634

A callback invoked when adding a breadcrumb, allowing to optionally modify
it before adding it to future events.

Note that you must return a valid breadcrumb from this callback. If you do
not wish to modify the breadcrumb, simply return it at the end.
Returning null will cause the breadcrumb to be dropped.

#### Parameters

##### breadcrumb

[`Breadcrumb`](../namespaces/Sentry/interfaces/Breadcrumb.md)

The breadcrumb as created by the SDK.

##### hint?

[`BreadcrumbHint`](../namespaces/Sentry/interfaces/BreadcrumbHint.md)

#### Returns

[`Breadcrumb`](../namespaces/Sentry/interfaces/Breadcrumb.md) \| `null`

The breadcrumb that will be added | null.

#### Inherited from

`Omit.beforeBreadcrumb`

---

### beforeSend?

> `optional` **beforeSend?**: (`event`, `hint`) => [`ErrorEvent`](../namespaces/Sentry/interfaces/ErrorEvent.md) \| `PromiseLike`\<[`ErrorEvent`](../namespaces/Sentry/interfaces/ErrorEvent.md) \| `null`\> \| `null`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:597

An event-processing callback for error and message events, guaranteed to be invoked after all other event
processors, which allows an event to be modified or dropped.

Note that you must return a valid event from this callback. If you do not wish to modify the event, simply return
it at the end. Returning `null` will cause the event to be dropped.

#### Parameters

##### event

[`ErrorEvent`](../namespaces/Sentry/interfaces/ErrorEvent.md)

The error or message event generated by the SDK.

##### hint

[`EventHint`](../namespaces/Sentry/interfaces/EventHint.md)

Event metadata useful for processing.

#### Returns

[`ErrorEvent`](../namespaces/Sentry/interfaces/ErrorEvent.md) \| `PromiseLike`\<[`ErrorEvent`](../namespaces/Sentry/interfaces/ErrorEvent.md) \| `null`\> \| `null`

A new event that will be sent | null.

#### Inherited from

`Omit.beforeSend`

---

### beforeSendLog?

> `optional` **beforeSendLog?**: (`log`) => [`Log`](../namespaces/Sentry/interfaces/Log.md) \| `null`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:543

An event-processing callback for logs, guaranteed to be invoked after all other log
processors. This allows a log to be modified or dropped before it's sent.

Note that you must return a valid log from this callback. If you do not wish to modify the log, simply return
it at the end. Returning `null` will cause the log to be dropped.

#### Parameters

##### log

[`Log`](../namespaces/Sentry/interfaces/Log.md)

The log generated by the SDK.

#### Returns

[`Log`](../namespaces/Sentry/interfaces/Log.md) \| `null`

A new log that will be sent | null.

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.beforeSendLog`

---

### beforeSendMetric?

> `optional` **beforeSendMetric?**: (`metric`) => [`Metric`](../namespaces/Sentry/interfaces/Metric.md) \| `null`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:573

An event-processing callback for metrics, guaranteed to be invoked after all other metric
processors. This allows a metric to be modified or dropped before it's sent.

Note that you must return a valid metric from this callback. If you do not wish to modify the metric, simply return
it at the end. Returning `null` will cause the metric to be dropped.

#### Parameters

##### metric

[`Metric`](../namespaces/Sentry/interfaces/Metric.md)

The metric generated by the SDK.

#### Returns

[`Metric`](../namespaces/Sentry/interfaces/Metric.md) \| `null`

A new metric that will be sent | null.

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.beforeSendMetric`

---

### beforeSendSpan?

> `optional` **beforeSendSpan?**: (`span`) => `SpanJSON` & `object`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:608

This function can be defined to modify a child span before it's sent.

When using `traceLifecycle: 'stream'`, wrap your callback with withStreamedSpan
to receive and return StreamedSpanJSON instead.

#### Type Declaration

##### \_streamed?

> `optional` **\_streamed?**: `true`

#### Param

**span**

The span generated by the SDK.

#### Returns

The modified span payload that will be sent.

#### Inherited from

`Omit.beforeSendSpan`

---

### beforeSendTransaction?

> `optional` **beforeSendTransaction?**: (`event`, `hint`) => `TransactionEvent` \| `PromiseLike`\<`TransactionEvent` \| `null`\> \| `null`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:622

An event-processing callback for transaction events, guaranteed to be invoked after all other event
processors. This allows an event to be modified or dropped before it's sent.

Note that you must return a valid event from this callback. If you do not wish to modify the event, simply return
it at the end. Returning `null` will cause the event to be dropped.

#### Parameters

##### event

`TransactionEvent`

The error or message event generated by the SDK.

##### hint

[`EventHint`](../namespaces/Sentry/interfaces/EventHint.md)

Event metadata useful for processing.

#### Returns

`TransactionEvent` \| `PromiseLike`\<`TransactionEvent` \| `null`\> \| `null`

A new event that will be sent | null.

#### Inherited from

`Omit.beforeSendTransaction`

---

### clientReportFlushInterval?

> `optional` **clientReportFlushInterval?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:78

Configures in which interval client reports will be flushed. Defaults to `60_000` (milliseconds).

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`clientReportFlushInterval`](../namespaces/Sentry/interfaces/NodeOptions.md#clientreportflushinterval)

---

### dataCollection?

> `optional` **dataCollection?**: `DataCollection`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:365

Controls what data the SDK collects and sends to Sentry.
All fields are optional — omitted fields use the documented defaults.

This replaces the deprecated ClientOptions.sendDefaultPii option and lets you control
each category of collected data (user info, cookies, headers, query params, request/response
bodies, gen AI inputs/outputs, etc.) individually.

#### Inherited from

`Omit.dataCollection`

---

### debug?

> `optional` **debug?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:158

Enable debug functionality in the SDK itself. If `debug` is set to `true` the SDK will attempt
to print out useful debugging information about what the SDK is doing.

#### Default

```ts
false;
```

#### Inherited from

`Omit.debug`

---

### defaultIntegrations?

> `optional` **defaultIntegrations?**: `false` \| `Integration`[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:654

If this is set to false, default integrations will not be added, otherwise this will internally be set to the
recommended default integrations.

#### Inherited from

`Omit.defaultIntegrations`

---

### denyUrls?

> `optional` **denyUrls?**: (`string` \| `RegExp`)[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:444

A pattern for error URLs which should not be sent to Sentry.
To allow certain errors instead, use [CoreOptions.allowUrls](#allowurls).
By default, all errors will be sent.

Behavior of the `denyUrls` option is controlled by the `Sentry.eventFiltersIntegration` integration.
If the event filters integration is not installed, the `denyUrls` option will not have any effect.

#### Default

```ts
[];
```

#### Inherited from

`Omit.denyUrls`

---

### disableInstrumentationWarnings?

> `optional` **disableInstrumentationWarnings?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:67

By default, the SDK will try to identify problems with your instrumentation setup and warn you about it.
If you want to disable these warnings, set this to `true`.

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`disableInstrumentationWarnings`](../namespaces/Sentry/interfaces/NodeOptions.md#disableinstrumentationwarnings)

---

### dist?

> `optional` **dist?**: `string`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:213

Sets the distribution of the application. Distributions are used to disambiguate build or
deployment variants of the same release of an application.

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.dist`

---

### enabled?

> `optional` **enabled?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:166

Specifies whether this SDK should send events to Sentry. Setting this to `enabled: false`
doesn't prevent all overhead from Sentry instrumentation. To disable Sentry completely,
depending on environment, call `Sentry.init conditionally.

#### Default

```ts
true;
```

#### Inherited from

`Omit.enabled`

---

### enableLogs?

> `optional` **enableLogs?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:530

If logs support should be enabled.

#### Default

```ts
true;
```

#### Inherited from

`Omit.enableLogs`

---

### enableMetrics?

> `optional` **enableMetrics?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:549

If metrics support should be enabled.

#### Default

```ts
true;
```

#### Inherited from

`Omit.enableMetrics`

---

### enhanceFetchErrorMessages?

> `optional` **enhanceFetchErrorMessages?**: `false` \| `"always"` \| `"report-only"`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:379

Controls whether and how to enhance fetch error messages by appending the request hostname.
Generic fetch errors like "Failed to fetch" will be enhanced to include the hostname
(e.g., "Failed to fetch (example.com)").

- `'always'` (default): Modifies the actual error message directly. This may break third-party packages
  that rely on exact message matching (e.g., is-network-error, p-retry).
- `'report-only'`: Only enhances the message when sending to Sentry. The original error
  message remains unchanged, preserving compatibility with third-party packages.
- `false`: Disables hostname enhancement completely.

#### Default

```ts
'always';
```

#### Inherited from

`Omit.enhanceFetchErrorMessages`

---

### environment?

> `optional` **environment?**: `string`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:206

The current environment of your application (e.g. "production").

Environments are case-sensitive. The environment name can't contain newlines, spaces or forward slashes,
can't be the string "None", or exceed 64 characters. You can't delete environments, but you can hide them.

#### Default

```ts
'production';
```

#### Inherited from

`Omit.environment`

---

### ignoreErrors?

> `optional` **ignoreErrors?**: (`string` \| `RegExp`)[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:314

A pattern for error messages which should not be sent to Sentry.
By default, all errors will be sent.

Behavior of the `ignoreErrors` option is controlled by the `Sentry.eventFiltersIntegration` integration. If the
event filters integration is not installed, the `ignoreErrors` option will not have any effect.

#### Default

```ts
[];
```

#### Inherited from

`Omit.ignoreErrors`

---

### ignoreSpans?

> `optional` **ignoreSpans?**: (`string` \| `RegExp` \| `IgnoreSpanFilter`)[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:334

A list of span names or patterns to ignore.

If you specify a pattern IgnoreSpanFilter, at least one
of the properties (`name`, `op`, or `attributes`) must be set.
When multiple properties are set, all must match for the span to be ignored.

#### Default

```ts
[];
```

#### Inherited from

`Omit.ignoreSpans`

---

### ignoreTransactions?

> `optional` **ignoreTransactions?**: (`string` \| `RegExp`)[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:324

A pattern for transaction names which should not be sent to Sentry.
By default, all transactions will be sent.

Behavior of the `ignoreTransactions` option is controlled by the `Sentry.eventFiltersIntegration` integration.
If the event filters integration is not installed, the `ignoreTransactions` option will not have any effect.

#### Default

```ts
[];
```

#### Inherited from

`Omit.ignoreTransactions`

---

### includeLocalVariables?

> `optional` **includeLocalVariables?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+node@10.71.0_@opentelemetry+core@2.10.0_supports-color@7.2.0/node_modules/@sentry/node/build/types/types.d.ts:52

Include local variables with stack traces.

Requires the `LocalVariables` integration.

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`includeLocalVariables`](../namespaces/Sentry/interfaces/NodeOptions.md#includelocalvariables)

---

### includeServerName?

> `optional` **includeServerName?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:62

If set to `false`, the SDK will not automatically detect the `serverName`.

This is useful if you are using the SDK in a CLI app or Electron where the
hostname might be considered PII.

#### Default

```ts
true;
```

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`includeServerName`](../namespaces/Sentry/interfaces/NodeOptions.md#includeservername)

---

### initialScope?

> `optional` **initialScope?**: [`CaptureContext`](../namespaces/Sentry/type-aliases/CaptureContext.md)

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:262

Initial data to populate scope.

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.initialScope`

---

### integrations?

> `optional` **integrations?**: `Integration`[] \| ((`integrations`) => `Integration`[])

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:660

List of integrations that should be installed after SDK was initialized.
Accepts either a list of integrations or a function that receives
default integrations and returns a new, updated list.

#### Inherited from

`Omit.integrations`

---

### maxBreadcrumbs?

> `optional` **maxBreadcrumbs?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:269

The maximum number of breadcrumbs sent with events.
Sentry has a maximum payload size of 1MB and any events exceeding that payload size will be dropped.

#### Default

```ts
100;
```

#### Inherited from

`Omit.maxBreadcrumbs`

---

### maxSpanWaitDuration?

> `optional` **maxSpanWaitDuration?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:88

The max. duration in seconds that the SDK will wait for parent spans to be finished before discarding a span.
The SDK will automatically clean up spans that have no finished parent after this duration.
This is necessary to prevent memory leaks in case of parent spans that are never finished or otherwise dropped/missing.
However, if you have very long-running spans in your application, a shorter duration might cause spans to be discarded too early.
In this case, you can increase this duration to a value that fits your expected data.

Defaults to 300 seconds (5 minutes).

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`maxSpanWaitDuration`](../namespaces/Sentry/interfaces/NodeOptions.md#maxspanwaitduration)

---

### maxValueLength?

> `optional` **maxValueLength?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:282

Maximum number of chars a single value can have before it will be truncated.

#### Inherited from

`Omit.maxValueLength`

---

### normalizeDepth?

> `optional` **normalizeDepth?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:293

Maximum number of levels that normalization algorithm will traverse in objects and arrays.
Used when normalizing an event before sending, on all of the listed attributes:

- `breadcrumbs.data`
- `user`
- `contexts`
- `extra`

#### Default

```ts
3;
```

#### Inherited from

`Omit.normalizeDepth`

---

### normalizeMaxBreadth?

> `optional` **normalizeMaxBreadth?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:304

Maximum number of properties or elements that the normalization algorithm will output in any single array or object included in the normalized event.
Used when normalizing an event before sending, on all of the listed attributes:

- `breadcrumbs.data`
- `user`
- `contexts`
- `extra`

#### Default

```ts
1000;
```

#### Inherited from

`Omit.normalizeMaxBreadth`

---

### onFatalError?

> `optional` **onFatalError?**: (`this`, `error`) => `void`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:92

Callback that is executed when a fatal global error occurs.

#### Parameters

##### this

`void`

##### error

`Error`

#### Returns

`void`

#### Inherited from

`Omit.onFatalError`

---

### openTelemetryBasicTracerProvider?

> `optional` **openTelemetryBasicTracerProvider?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+node-core@10.71.0_@opentelemetry+api@1.9.1_@opentelemetry+core@2.10.0_@opentele_7442c4ce560fd457e5b8f798e43d1911/node_modules/@sentry/node-core/build/types/types.d.ts:47

By default, the SDK uses Sentry's minimal OpenTelemetry tracer provider, which creates native
Sentry spans directly instead of going through the full OpenTelemetry SDK span pipeline.

Set this to `true` to use the full OpenTelemetry SDK `BasicTracerProvider` instead, e.g. if you
rely on OpenTelemetry SDK features that the minimal provider does not support.

Note: providing `openTelemetrySpanProcessors` also forces the full OpenTelemetry SDK provider,
since custom span processors require the SDK span pipeline.

#### Default

```ts
false;
```

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`openTelemetryBasicTracerProvider`](../namespaces/Sentry/interfaces/NodeOptions.md#opentelemetrybasictracerprovider)

---

### openTelemetryInstrumentations?

> `optional` **openTelemetryInstrumentations?**: `Instrumentation`\<`InstrumentationConfig`\>[]

Defined in: node_modules/.pnpm/@sentry+node-core@10.71.0_@opentelemetry+api@1.9.1_@opentelemetry+core@2.10.0_@opentele_7442c4ce560fd457e5b8f798e43d1911/node_modules/@sentry/node-core/build/types/types.d.ts:26

Provide an array of OpenTelemetry Instrumentations that should be registered.

Use this option if you want to register OpenTelemetry instrumentation that the Sentry SDK does not yet have support for.

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`openTelemetryInstrumentations`](../namespaces/Sentry/interfaces/NodeOptions.md#opentelemetryinstrumentations)

---

### openTelemetrySpanProcessors?

> `optional` **openTelemetrySpanProcessors?**: `SpanProcessor`[]

Defined in: node_modules/.pnpm/@sentry+node-core@10.71.0_@opentelemetry+api@1.9.1_@opentelemetry+core@2.10.0_@opentele_7442c4ce560fd457e5b8f798e43d1911/node_modules/@sentry/node-core/build/types/types.d.ts:34

Provide an array of additional OpenTelemetry SpanProcessors that should be registered.

Note: providing this forces the full OpenTelemetry SDK `BasicTracerProvider` instead of Sentry's
minimal tracer provider, since custom span processors require the SDK span pipeline. See
[OpenTelemetryServerRuntimeOptions.openTelemetryBasicTracerProvider](../namespaces/Sentry/interfaces/NodeOptions.md#opentelemetrybasictracerprovider).

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`openTelemetrySpanProcessors`](../namespaces/Sentry/interfaces/NodeOptions.md#opentelemetryspanprocessors)

---

### orgId?

> `optional` **orgId?**: `number` \| `` `${number}` ``

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:511

The organization ID for your Sentry project.

The SDK will try to extract the organization ID from the DSN. If it cannot be found, or if you need to override it,
you can provide the ID with this option. The organization ID is used for trace propagation and for features like `strictTraceContinuation`.

#### Inherited from

`Omit.orgId`

---

### parentSpanIsAlwaysRootSpan?

> `optional` **parentSpanIsAlwaysRootSpan?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:256

If this is enabled, any spans started will always have their parent be the active root span,
if there is any active span.

This is necessary because in some environments (e.g. browser),
we cannot guarantee an accurate active span.
Because we cannot properly isolate execution environments,
you may get wrong results when using e.g. nested `startSpan()` calls.

To solve this, in these environments we'll by default enable this option.

#### Inherited from

`Omit.parentSpanIsAlwaysRootSpan`

---

### profileLifecycle?

> `optional` **profileLifecycle?**: `"manual"` \| `"trace"`

Defined in: node_modules/.pnpm/@sentry+node@10.71.0_@opentelemetry+core@2.10.0_supports-color@7.2.0/node_modules/@sentry/node/build/types/types.d.ts:46

Set the lifecycle mode of the profiler.

- **manual**: The profiler will be manually started and stopped via `startProfiler`/`stopProfiler`.
  If a session is sampled, is dependent on the `profileSessionSampleRate`.
- **trace**: The profiler will be automatically started when a root span exists and stopped when there are no
  more sampled root spans. Whether a session is sampled, is dependent on the `profileSessionSampleRate` and the
  existing sampling configuration for tracing (`tracesSampleRate`/`tracesSampler`).

#### Default

```ts
'manual';
```

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`profileLifecycle`](../namespaces/Sentry/interfaces/NodeOptions.md#profilelifecycle)

---

### profileSessionSampleRate?

> `optional` **profileSessionSampleRate?**: `number`

Defined in: node_modules/.pnpm/@sentry+node@10.71.0_@opentelemetry+core@2.10.0_supports-color@7.2.0/node_modules/@sentry/node/build/types/types.d.ts:35

Sets profiling session sample rate for the entire profiling session (evaluated once per SDK initialization).

#### Default

```ts
0;
```

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`profileSessionSampleRate`](../namespaces/Sentry/interfaces/NodeOptions.md#profilesessionsamplerate)

---

### ~~profilesSampler?~~

> `optional` **profilesSampler?**: (`samplingContext`) => `number` \| `boolean`

Defined in: node_modules/.pnpm/@sentry+node@10.71.0_@opentelemetry+core@2.10.0_supports-color@7.2.0/node_modules/@sentry/node/build/types/types.d.ts:29

Function to compute profiling sample rate dynamically and filter unwanted profiles.

Profiling is enabled if either this or `profilesSampleRate` is defined. If both are defined, `profilesSampleRate` is
ignored.

Will automatically be passed a context object of default and optional custom data.

#### Parameters

##### samplingContext

`SamplingContext`

#### Returns

`number` \| `boolean`

A sample rate between 0 and 1 (0 drops the profile, 1 guarantees it will be sent). Returning `true` is
equivalent to returning 1 and returning `false` is equivalent to returning 0.

#### Deprecated

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`profilesSampler`](../namespaces/Sentry/interfaces/NodeOptions.md#profilessampler)

---

### ~~profilesSampleRate?~~

> `optional` **profilesSampleRate?**: `number`

Defined in: node_modules/.pnpm/@sentry+node@10.71.0_@opentelemetry+core@2.10.0_supports-color@7.2.0/node_modules/@sentry/node/build/types/types.d.ts:15

Sets profiling sample rate when @sentry/profiling-node is installed

#### Deprecated

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`profilesSampleRate`](../namespaces/Sentry/interfaces/NodeOptions.md#profilessamplerate)

---

### propagateTraceparent?

> `optional` **propagateTraceparent?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:485

If set to `true`, the SDK propagates the W3C `traceparent` header to any outgoing requests,
in addition to the `sentry-trace` and `baggage` headers. Use the [CoreOptions.tracePropagationTargets](#tracepropagationtargets)
option to control to which outgoing requests the header will be attached.

**Important:** If you set this option to `true`, make sure that you configured your servers'
CORS settings to allow the `traceparent` header. Otherwise, requests might get blocked.

#### See

https://www.w3.org/TR/trace-context/

#### Default

```ts
false;
```

#### Inherited from

`Omit.propagateTraceparent`

---

### registerEsmLoaderHooks?

> `optional` **registerEsmLoaderHooks?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+node@10.71.0_@opentelemetry+core@2.10.0_supports-color@7.2.0/node_modules/@sentry/node/build/types/types.d.ts:61

Whether to register ESM loader hooks to automatically instrument libraries.
This is necessary to auto instrument libraries that are loaded via ESM imports, but it can cause issues
with certain libraries. If you run into problems running your app with this enabled,
please raise an issue in https://github.com/getsentry/sentry-javascript.

Defaults to `true`.

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`registerEsmLoaderHooks`](../namespaces/Sentry/interfaces/NodeOptions.md#registeresmloaderhooks)

---

### release?

> `optional` **release?**: `string`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:197

Sets the release. Release names are strings, but some formats are detected by Sentry and might be
rendered differently. Learn more about how to send release data so Sentry can tell you about
regressions between releases and identify the potential source in the
[releases documentation](https://docs.sentry.io/product/releases/)

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.release`

---

### root?

> `optional` **root?**: `string` \| `URL`

Defined in: [packages/shared-http-pieces/src/lib/sentry.ts:52](https://github.com/wolfstar-project/stars-components/blob/f7b31cc0eb5f9a284e64590e28eadbf470e88f64/packages/shared-http-pieces/src/lib/sentry.ts#L52)

---

### sampleRate?

> `optional` **sampleRate?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:278

A global sample rate to apply to all error events.

0.0 = 0% chance of a given event being sent (send no events) 1.0 = 100% chance of a given event being sent (send
all events)

#### Default

```ts
1.0;
```

#### Inherited from

`Omit.sampleRate`

---

### sendClientReports?

> `optional` **sendClientReports?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:182

Send SDK Client Reports, which are used to emit outcomes about events that the SDK dropped
or failed to capture.

#### Default

```ts
true;
```

#### Inherited from

`Omit.sendClientReports`

---

### ~~sendDefaultPii?~~

> `optional` **sendDefaultPii?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:356

Controls if potentially sensitive data should be sent to Sentry by default.
Note that this only applies to data that the SDK is sending by default
but not data that was explicitly set (e.g. by calling `Sentry.setUser()`).

#### Default

```ts
false;
```

#### Deprecated

Use the ClientOptions.dataCollection option instead, which lets you control
each category of collected data individually. `sendDefaultPii` will be removed in the next major
version (v11). For backwards compatibility, setting `sendDefaultPii: true` currently behaves like
enabling all `dataCollection` categories. If both `sendDefaultPii` and `dataCollection` are set,
`sendDefaultPii` will be ignored.

#### Inherited from

`Omit.sendDefaultPii`

---

### serverName?

> `optional` **serverName?**: `string`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:42

Sets an optional server name (device name).

This is useful for identifying which server or instance is sending events.

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`serverName`](../namespaces/Sentry/interfaces/NodeOptions.md#servername)

---

### shutdownTimeout?

> `optional` **shutdownTimeout?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:74

Controls how many milliseconds to wait before shutting down. The default is 2 seconds. Setting this too low can cause
problems for sending events from command line applications. Setting it too
high can cause the application to block for users with network connectivity
problems.

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`shutdownTimeout`](../namespaces/Sentry/interfaces/NodeOptions.md#shutdowntimeout)

---

### skipOpenTelemetrySetup?

> `optional` **skipOpenTelemetrySetup?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+node-core@10.71.0_@opentelemetry+api@1.9.1_@opentelemetry+core@2.10.0_@opentele_7442c4ce560fd457e5b8f798e43d1911/node_modules/@sentry/node-core/build/types/types.d.ts:20

If this is set to true, the SDK will not set up OpenTelemetry automatically.
In this case, you _have_ to ensure to set it up correctly yourself, including:

- The `SentrySpanProcessor`
- The `SentryPropagator`
- The `SentryContextManager`
- The `SentrySampler`

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`skipOpenTelemetrySetup`](../namespaces/Sentry/interfaces/NodeOptions.md#skipopentelemetrysetup)

---

### spotlight?

> `optional` **spotlight?**: `string` \| `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:53

If you use Spotlight by Sentry during development, use
this option to forward captured Sentry events to Spotlight.

Either set it to true, or provide a specific Spotlight Sidecar URL.

More details: https://spotlightjs.com/

IMPORTANT: Only set this option to `true` while developing, not in production!

#### Inherited from

[`NodeOptions`](../namespaces/Sentry/interfaces/NodeOptions.md).[`spotlight`](../namespaces/Sentry/interfaces/NodeOptions.md#spotlight)

---

### stackParser?

> `optional` **stackParser?**: `StackParser` \| `StackLineParser`[]

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:671

A stack parser implementation or an array of stack line parsers
By default, a stack parser is supplied for all supported browsers

#### Inherited from

`Omit.stackParser`

---

### streamGenAiSpans?

> `optional` **streamGenAiSpans?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:524

Unless set to `false`, gen_ai spans will be extracted from transactions and sent as v2 span envelope items.

This enables streaming gen_ai spans, avoiding payload size limits of usual transactions.

Because the v2 span format is not subject to the transaction payload-size limits that gen_ai message
truncation exists to work around, this also disables gen_ai input truncation by default. Set
`enableTruncation: true` on the respective AI integration to opt back into truncation, or set this
option to `false` to send gen_ai spans as part of the transaction (which re-enables truncation by default).

#### Default

```ts
true;
```

#### Inherited from

`Omit.streamGenAiSpans`

---

### strictTraceContinuation?

> `optional` **strictTraceContinuation?**: `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:497

If set to `true`, the SDK will only continue a trace if the `organization ID` of the incoming trace found in the
`baggage` header matches the `organization ID` of the current Sentry client.

The client's organization ID is extracted from the DSN or can be set with the `orgId` option.

If the organization IDs do not match, the SDK will start a new trace instead of continuing the incoming one.
This is useful to prevent traces of unknown third-party services from being continued in your application.

#### Default

```ts
false;
```

#### Inherited from

`Omit.strictTraceContinuation`

---

### traceLifecycle?

> `optional` **traceLifecycle?**: `"static"` \| `"stream"`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:504

The trace lifecycle, determining whether spans are sent statically when the entire local span tree is complete,
or streamed in batches, following interval- and action-based triggers.

#### Default

```ts
'static';
```

#### Inherited from

`Omit.traceLifecycle`

---

### tracePropagationTargets?

> `optional` **tracePropagationTargets?**: `TracePropagationTargets`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:472

List of strings and/or Regular Expressions used to determine which outgoing requests will have `sentry-trace` and `baggage`
headers attached.

**Default:** If this option is not provided, tracing headers will be attached to all outgoing requests.
If you are using a browser SDK, by default, tracing headers will only be attached to outgoing requests to the same origin.

**Disclaimer:** Carelessly setting this option in browser environments may result into CORS errors!
Only attach tracing headers to requests to the same origin, or to requests to services you can control CORS headers of.
Cross-origin requests, meaning requests to a different domain, for example a request to `https://api.example.com/` while you're on `https://example.com/`, take special care.
If you are attaching headers to cross-origin requests, make sure the backend handling the request returns a `"Access-Control-Allow-Headers: sentry-trace, baggage"` header to ensure your requests aren't blocked.

If you provide a `tracePropagationTargets` array, the entries you provide will be matched against the entire URL of the outgoing request.
If you are using a browser SDK, the entries will also be matched against the pathname of the outgoing requests.
This is so you can have matchers for relative requests, for example, `/^/api/` if you want to trace requests to your `/api` routes on the same domain.

If any of the two match any of the provided values, tracing headers will be attached to the outgoing request.
Both, the string values, and the RegExes you provide in the array will match if they partially match the URL or pathname.

Examples:

- `tracePropagationTargets: [/^/api/]` and request to `https://same-origin.com/api/posts`:
    - Tracing headers will be attached because the request is sent to the same origin and the regex matches the pathname "/api/posts".
- `tracePropagationTargets: [/^/api/]` and request to `https://different-origin.com/api/posts`:
    - Tracing headers will not be attached because the pathname will only be compared when the request target lives on the same origin.
- `tracePropagationTargets: [/^/api/, 'https://external-api.com']` and request to `https://external-api.com/v1/data`:
    - Tracing headers will be attached because the request URL matches the string `'https://external-api.com'`.

#### Inherited from

`Omit.tracePropagationTargets`

---

### tracesSampler?

> `optional` **tracesSampler?**: (`samplingContext`) => `number` \| `boolean`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:585

Function to compute tracing sample rate dynamically and filter unwanted traces.

Tracing is enabled if either this or `tracesSampleRate` is defined. If both are defined, `tracesSampleRate` is
ignored. Set this and `tracesSampleRate` to `undefined` to disable tracing.

Will automatically be passed a context object of default and optional custom data.

#### Parameters

##### samplingContext

`TracesSamplerSamplingContext`

#### Returns

`number` \| `boolean`

A sample rate between 0 and 1 (0 drops the trace, 1 guarantees it will be sent). Returning `true` is
equivalent to returning 1 and returning `false` is equivalent to returning 0.

#### Inherited from

`Omit.tracesSampler`

---

### tracesSampleRate?

> `optional` **tracesSampleRate?**: `number`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:244

Sample rate to determine trace sampling.

0.0 = 0% chance of a given trace being sent (send no traces) 1.0 = 100% chance of a given trace being sent (send
all traces).

Tracing is enabled if either this or `tracesSampler` is defined. If both are defined, `tracesSampleRate` is
ignored. Set this and `tracesSampler` to `undefined` to disable tracing.

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.tracesSampleRate`

---

### transport?

> `optional` **transport?**: (`transportOptions`) => `Transport`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:666

A function that takes transport options and returns the Transport object which is used to send events to Sentry.
The function is invoked internally during SDK initialization.
By default, the SDK initializes its default transports.

#### Parameters

##### transportOptions

`NodeTransportOptions`

#### Returns

`Transport`

#### Inherited from

`Omit.transport`

---

### transportOptions?

> `optional` **transportOptions?**: `Partial`\<`NodeTransportOptions`\>

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:232

Options for the default transport that the SDK uses.

#### Inherited from

`Omit.transportOptions`

---

### tunnel?

> `optional` **tunnel?**: `string`

Defined in: node_modules/.pnpm/@sentry+core@10.71.0/node_modules/@sentry/core/build/types/types/options.d.ts:342

A URL to an envelope tunnel endpoint. An envelope tunnel is an HTTP endpoint
that accepts Sentry envelopes for forwarding. This can be used to force data
through a custom server independent of the type of data.

#### Default

```ts
undefined;
```

#### Inherited from

`Omit.tunnel`
